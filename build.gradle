apply plugin: 'groovy'
apply plugin: 'maven'

group 'us.phildop'
version '0.5'

dependencies {
  compile gradleApi()
  groovy localGroovy()
  testCompile 'junit:junit:4.10'
}

repositories {
  mavenCentral()
}
